# packages/

Reserved for **shared libraries** used by more than one app (e.g. types, UI kit). Not an npm workspace yet — add a `package.json` here and include the path in the root `workspaces` array when you introduce a package.
