# Admin dashboard moved

The internal admin dashboard (`admin-web`) now lives in the Mustidev monorepo:

```
mustidev/apps/dashboard
```

Run it from the Mustidev workspace root:

```bash
cd ../mustidev
npm run dashboard:dev
```

Portfolio remains in this repo: `apps/portfolio-web`.
