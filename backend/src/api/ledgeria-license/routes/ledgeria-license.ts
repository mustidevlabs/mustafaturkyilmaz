/**
 * ledgeria-license router — REST under /api/ledgeria-licenses (keep Public role closed).
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreRouter('api::ledgeria-license.ledgeria-license');
