/**
 * ledgeria-edition router — REST under /api/ledgeria-editions (keep Public role closed).
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreRouter('api::ledgeria-edition.ledgeria-edition');
