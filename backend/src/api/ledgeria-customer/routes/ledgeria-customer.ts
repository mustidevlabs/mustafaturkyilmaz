/**
 * ledgeria-customer router — REST under /api/ledgeria-customers (keep Public role closed).
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreRouter('api::ledgeria-customer.ledgeria-customer');
